version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: encurtador-db
    environment:
     POSTGRES_DB: urlshortener
     POSTGRES_USER: admin
     POSTGRES_PASSWORD: admin123
    ports:
     - "5432:5432"

  redis:
    image: redis:7
    ports:
      - "6379:6379"